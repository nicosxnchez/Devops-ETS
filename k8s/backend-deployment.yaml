apiVersion: apps/v1
kind: Deployment
metadata:
  name: despachos-backend
  namespace: despachos
spec:
  replicas: 2
  selector:
    matchLabels:
      app: despachos-backend
  template:
    metadata:
      labels:
        app: despachos-backend
    spec:
      containers:
        - name: backend
          image: {{ECR_URL}}/despachos-backend:eks-v1
          ports:
            - containerPort: 3001
          env:
            - name: DB_HOST
              value: "despachos-db"
            - name: DB_USER
              value: "root"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_ROOT_PASSWORD
            - name: DB_NAME
              value: "despachos_db"
            - name: DB_PORT
              value: "3306"

          # Se agrega Resource para que Metrics Server calcule el % de CPU y Memoria para escalar 
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          # HEALTH CHECKS (PROBES)
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /api/health
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 10